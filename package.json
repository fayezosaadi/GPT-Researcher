{
  "name": "gpt-researcher",
  "version": "1.0.0",
  "description": "GPT-Researcher is a simplified backend service that receives user queries, performs web searches, analyzes the content from the search results, and returns a concise summary using OpenAI's Language Model.",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --exec ts-node -r dotenv/config index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "express": "^4.18.2",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@googleapis/customsearch": "^1.0.0",
    "axios": "^1.4.0",
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.3.1",
    "natural": "^6.5.0",
    "openai": "^3.3.0"
  }
}
